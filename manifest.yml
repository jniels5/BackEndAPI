---
applications:
- name: campus-innovator-api
  memory: 512mb
  instances: 1
  buildpacks:
  - nodejs_buildpack
  env:
    OPTIMIZE_MEMORY: true
  services:
  - campus-innovator-db
  stack: cflinuxfs3
  command: npm start
